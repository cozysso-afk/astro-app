# Viewport background v61

Regression target: iOS native select / browser chrome must not move the app aurora. Vertical anchors are fixed pixel values; viewport-height units are forbidden in the final background owner.
