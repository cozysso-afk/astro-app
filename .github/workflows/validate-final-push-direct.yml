name: Completed direct push validation (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - name: Validation already completed
        run: echo "Direct OneSignal sender validation already completed successfully."
