name: Publish Emergency Mobile Build

on:
  push:
    branches:
      - main
    paths:
      - 'web/**'
      - '.github/workflows/publish-emergency-mobile.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Build latest mobile app
        working-directory: web
        run: |
          npm ci
          npm run build -- --base=/astro-app/mobile/
      - name: Publish into Pages-served tree
        shell: bash
        run: |
          rm -rf mobile
          mkdir -p mobile
          cp -R web/dist/. mobile/
          touch mobile/.nojekyll
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f mobile
          if git diff --cached --quiet; then
            echo 'Mobile build is already current.'
            exit 0
          fi
          git commit -m 'deploy: refresh mobile frontend on GitHub Pages'
          git pull --rebase origin main
          git push origin HEAD:main
