services:
  - type: web
    name: astro-app-api
    runtime: python
    plan: free
    buildCommand: pip install -r api/requirements.txt
    startCommand: uvicorn api.private_app:app --host 0.0.0.0 --port $PORT
    autoDeploy: false
    healthCheckPath: /health
