# 별빛의 운명 — 홈화면 로그인·유리 테마·계산 점검

기준: main `5f73587808e1349fb9a0358d04087873d694a2aa`.
상태: 검토용 변경. 운영 배포 및 실제 iPhone 인증/시각 검증 미완료.

## 홈화면 로그인

manifest의 display=standalone, start_url=/, scope=/ 및 Apple standalone meta는 이미 정상이다. 기존 AuthGate는 메일 링크를 누르는 경로만 제공하며, 메일 앱에서 열린 브라우저의 인증을 홈화면 앱으로 이전할 방법이 없다. 제공된 새 설치 URL과 실제 iOS 리다이렉트는 확보하지 못했으므로 도메인 변경까지 원인으로 확정하지 않는다.

최종 변경 경로: 앱에서 이메일 입력 → 메일의 숫자 인증번호 입력 → 같은 앱 문맥에서 Supabase verifyOtp 검증 → 서버 allowlist 확인. 앞선 링크 붙여넣기 경로는 사용자의 요청에 따라 제거했다.

일반 로그인은 shouldCreateUser=false 및 type=email, 기존 익명 계정 연결은 type=email_change를 사용한다. 기존 UUID·email 보호와 클라우드 기록 검사를 유지한다. 번호는 저장하지 않고 제출 시 React state에서 지운다. 재전송은 60초 간격, sessionStorage에는 이메일·요청 시각만 저장하여 메일 확인 후 입력 화면을 복구한다. 만료/잘못된 번호/과다 시도 메시지와 인증 성공 후 접근 확인 재시도를 포함한다.

**운영 전환 미완료:** Supabase Magic Link와 Change Email 템플릿에 {{ .Token }}을 넣어야 한다. `supabase/templates/`에 완성된 HTML과 정확한 관리 API patch를 준비했다. 연결된 Supabase 도구는 Auth 설정 변경을 제공하지 않고, 관리 토큰도 없으며, Cloud Browser는 연결 timeout이다. 따라서 이메일 템플릿 적용, 실제 메일 수신과 iPhone 검증, 운영 배포는 수행하지 않았다. UI만 먼저 배포하면 기존 링크 메일에 입력할 번호가 없을 수 있으므로 보류했다.

manifest에 안정적인 id=/를 명시했다. 설정만으로 메일 앱에서 홈화면 앱으로 강제 복귀한다고 주장하지 않는다. 기존 앱 삭제나 저장 데이터 초기화는 필요하지 않다.

## 디자인·해설

기존 미커밋 연락 문장 개선을 보존. Opal 유리 표면에 로즈·하늘·살구·민트·라일락을 분산하고, 선택 상태는 테두리로도 구별한다. 카드 내부 반사광과 짧은 등장/눌림 효과, reduced-motion 대응. 지속적으로 움직이는 입자·타이머는 추가하지 않았다. 간격을 줄이고 본문 대비를 유지하도록 구성했다.

화면 점검용 Cloud Browser가 탭 목록 조회 timeout과 새 탭 생성 timeout으로 실패했다. 따라서 실제 CSS 합성/스크롤/터치/물리 iPhone Safari 검증은 미완료다. 이전 QA의 성공을 이번 변경의 성공으로 재사용하지 않았다.

## 계산 기반 평가

로컬 계산 파일은 변경하지 않았다. 이번 테스트 결과는 현재 소스와 로컬 계산 의존성에 해당하며 운영 Render의 동일 버전 실행 증거를 대신하지 않는다.

- 핵심 10개 테스트 파일: 77 passed, 16 subtests passed.
- 추가 12개 테스트 파일: 112 passed, 15 subtests passed.
- 합계: 189 passed, 31 subtests passed. 건너뛴 테스트는 보고되지 않았다.
- JPL 기준: 저장된 J2000 및 2024 입춘 2개 시점, 10개 천체 경도 비교, 허용 오차 2 arcsec. 모든 시대·좌표·천체 전체 정확성을 입증한 것은 아니다.
- 절기: 2024년 12절 기준값 및 분수 시간대 변환, 경계 허용 오차 120초.
- 사주: 절입 전후, 진태양시 날짜 이동, 자시 정책, 4주 기준값 확인. 채택한 유파 규칙에 대한 일관성이며 유파 전체의 유일 정답이라는 뜻은 아니다.
- 서양: 하우스·극지/시간대·중간점·시기·출생시간 신뢰도·관계 계산 검사를 포함.
- 태국: 기존 MyHora 라그나 기준표 및 경계, 애스펙트·디그니티·하우스 검사 포함. 현대 서양 천문 좌표와 전통 계산법을 동일한 정확도 개념으로 합치지 않는다.

## 확인된 미완성 범위

1. `integrated_fortune_v1.py`는 신강·신약, 용신·희신·기신, 형·파·해 전체 자동판정을 명시적으로 not_calculated로 반환한다.
2. 사주 기간 해설은 월운·세운의 천간 십성을 5개 해설군으로 연결한다. 연애 해설을 실제 계산된 5개 십성군 모두에 연결하고, 월운·세운별 간지와 십성을 구분했다. 이는 상징적인 생활 해설이며 배우자성·상대 마음·사건 예측의 추가 계산이 아니다. 컨디션은 연결 키가 비어 있다.
3. 태국 요약은 현재 기간 배치를 기본으로 표시하고 같은 생활 영역의 출생/현재 행성 변화를 구체적으로 비교하며 최종 길흉·정확한 사건 시각 판정은 제공하지 않는다.
4. 연락/재회 등 분야 점수는 수동 정의 가중치·역행 보정·합산 규칙에 기반한다. 예: 수성/금성 역행 재회 활성도 배율 1.10. 이 값은 실증된 사건 확률이나 적중률이 아니다.
5. 기존 외부 기준표의 연도/지역 범위 밖은 추가 검증이 필요하다. 통과 수만으로 예측 성능을 평가하지 않는다.

판정: 천문·역법 및 입력 정밀도 처리는 근거를 제시할 수 있는 수준. 사주·태국의 종합 해설 범위와 사건 예측 신뢰도까지 완성형이라고 표현할 수 없다.

## 해설 수정 및 다섯 관점 검토

1. 인증 경계: 요청/검증, 만료·과다 시도, 이메일/UUID 불일치, 익명 기록 보존, 재전송·입력 화면 복구. Mock 검증이며 실메일 검증 아님.
2. 근거 방향: contribution은 크기이고 polarity가 방향이라는 계산 계약에 맞춰 수정. 방향이 없으면 긍정으로 추정하지 않는다.
3. 연락 방향과 관계 상태: 받는 연락/먼저 하는 연락을 독립 해석. 싱글·썸 등 문맥 적용 뒤에도 계산된 방향·시기와 연락 판단을 보존한다.
4. 시기와 체계: 서로 다른 날짜의 근거를 동시에 겹친다고 표현하지 않는다. 사주는 실제 월운·세운/십성, 태국은 현재/출생 행성을 비교한다.
5. 해설 품질/화면 출력: 서버 품질 검사를 통과하고 근거 참조가 있는 AI 해설을 보존한다. 임시·저품질 결과는 기존 결정론적 대체 해설을 사용한다. 서양 전용 화면에는 혼합 체계 문장을 넣지 않는다.

검증: TypeScript + Vite build 통과. 기간/관계/체계별 React SSR·회귀 82 tests passed. 신규 OTP/해설 신뢰성 및 기존 인증 계약 검사 통과. 실제 화면 합성·iPhone 실기기 검증은 브라우저 연결 실패로 미완료. 계산 엔진은 변경하지 않았으며 앞선 계산 검사 결과는 위에 별도 기재했다.

참고: https://supabase.com/docs/guides/auth/auth-email-passwordless 및 https://supabase.com/docs/reference/javascript/auth-verifyotp 및 https://supabase.com/docs/guides/auth/auth-email-templates
